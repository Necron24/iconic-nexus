import { redirect } from "next/navigation";
import { createClient } from "@/lib/supabase/server";
import { resolveReport } from './actions';
export default async function AdminReportsPage(){const supabase=await createClient();const {data:{user}}=await supabase.auth.getUser();if(!user)redirect('/login');const {data:p}=await supabase.from('profiles').select('is_admin').eq('id',user.id).single();if(!p?.is_admin)redirect('/dashboard');const {data:reports}=await supabase.from('reports').select('id,target_type,target_id,reason,details,resolved,created_at,profiles!reports_reporter_id_fkey(username)').order('created_at',{ascending:false}).limit(100);return <div><p className="text-sm font-bold uppercase tracking-[.2em] text-cyan">Administration</p><h2 className="mt-2 text-3xl font-black">Moderation reports</h2><div className="mt-6 space-y-4">{(reports||[]).map((r:any)=><article key={r.id} className="card p-5"><div className="flex flex-wrap items-start justify-between gap-4"><div><p className="font-black">{r.reason}</p><p className="mt-1 text-sm text-soft">{r.target_type} · {r.target_id} · reported by @{r.profiles?.username||'unknown'}</p></div><span className="badge">{r.resolved?'Resolved':'Open'}</span></div>{r.details&&<p className="mt-4 whitespace-pre-wrap text-soft">{r.details}</p>}{!r.resolved&&<form action={resolveReport} className="mt-4"><input type="hidden" name="id" value={r.id}/><button className="btn-primary !px-4 !py-2">Mark resolved</button></form>}</article>)}{!reports?.length&&<div className="card p-8 text-center text-soft">No reports yet.</div>}</div></div>}

"use server";
import { redirect } from "next/navigation";
import { createClient } from "@/lib/supabase/server";
export async function submitReport(formData:FormData){const targetType=String(formData.get('targetType')||'').trim();const targetId=String(formData.get('targetId')||'').trim();const reason=String(formData.get('reason')||'').trim();const details=String(formData.get('details')||'').trim();if(!['project','campaign','profile','feedback'].includes(targetType)||!targetId||!reason)redirect('/report?error=Invalid%20report.');if(details.length>2000)redirect(`/report?targetType=${targetType}&targetId=${targetId}&error=Details%20are%20too%20long.`);const supabase=await createClient();const {data:{user}}=await supabase.auth.getUser();if(!user)redirect('/login?error=Log%20in%20to%20submit%20a%20report.');const {error}=await supabase.from('reports').insert({reporter_id:user.id,target_type:targetType,target_id:targetId,reason,details:details||null});if(error)redirect(`/report?targetType=${targetType}&targetId=${targetId}&error=${encodeURIComponent(error.message)}`);redirect('/report?success=Your%20report%20has%20been%20submitted.');}
